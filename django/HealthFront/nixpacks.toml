[phases.install]
cmds = ["pip install -r ../../requirements.txt"]

[start]
cmd = "python manage.py collectstatic --noinput && gunicorn HealthFront.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
